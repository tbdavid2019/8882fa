# ========================================================
# 888 2FA - 本地部署環境變數範本 (.env.example)
#
# 使用方式：複製此檔案為 .env 並填寫您的資訊（.env 已被 gitignore 忽略）
#   cp .env.example .env
# ========================================================

# Cloudflare 帳戶 ID（選填）
# 若您的 Cloudflare 登入關聯多個帳號，設定此項可避免每次部署時跳出互動式選擇提示
# 可在 Cloudflare Dashboard 網址中找到（如 dash.cloudflare.com/<account_id>）
# CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id

# 自訂網域（選填）
# 若您在 Cloudflare Dashboard 為本 Worker 綁定了自訂網域（如 2fa.yourdomain.com），
# 設定此變數後，deploy 腳本會在發布版本時原子性綁定並預熱自訂網域，徹底消除邊緣路由空窗期 (403)
# CUSTOM_DOMAIN=2fa.yourdomain.com
